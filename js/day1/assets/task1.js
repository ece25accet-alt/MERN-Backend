
var alpha = 1;
let beta = 2;
const pi = 3.1452;

alpha = 2;
var alpha = 4; //Var can be re-declared and re-assigned
beta = 5; //let variable can be re assigned but it won't be re-declared

//const is a constant so it will neither be re-declarred nor be re-assigned
console.log(alpha);
console.log(beta);
console.log(pi);
